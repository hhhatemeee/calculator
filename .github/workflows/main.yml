
name: tests

on:
  push:
    branches: feature/calc-16 
  pull_request:
    branches: feature/calc-16 

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
          - uses: actions/checkout@v1
          - name: Run Jest
            uses: stefanoeb/jest-action@1.0.3
