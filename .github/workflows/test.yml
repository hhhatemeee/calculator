name: CI

on:
  push:
    branches: 
    - feature/CALC-30
  pull_request:
    branches: 
    - release/2.3.0

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      
      - name: install
        run: npm install
        
      - name: Run TEST
        run: npm run test
        env:
          GITHUB_TOKEN: ${{ secrets.TEST }}